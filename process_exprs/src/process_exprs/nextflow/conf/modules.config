// Config file for module options

params {
    modules {
        'update_pact_emat' {
            publish_dir        = "update_pact_emat"
        }
        'batch_correct' {
            publish_dir        = "batch_correct"
        }
    }
}