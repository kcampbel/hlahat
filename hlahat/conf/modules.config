// Config file for module options

params {
    modules {
        'extract_reads' {
            publish_dir = "extracted_reads"
        }
        'hisat_genotype' {
            publish_dir = "genotype"
        }
    }
}